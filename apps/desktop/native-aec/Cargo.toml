[package]
name = "graneri-aec"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
webrtc-audio-processing = { version = "2.1.0", features = ["bundled", "experimental-aec3-config"] }
